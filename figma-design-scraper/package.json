{
  "name": "design-value-scraper",
  "version": "1.0.0",
  "description": "Figma plugin that exports a deduplicated, inference-free catalog of raw design values to CSV.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.4.0"
  }
}
